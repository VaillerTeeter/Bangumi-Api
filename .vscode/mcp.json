{
  "inputs": [
    {
      "id": "github-token",
      "type": "promptString",
      "description": "GitHub Personal Access Token (同 .env 中的 GH_TOKEN)",
      "password": true
    }
  ],
  "servers": {
    "github": {
      "type": "stdio",
      "command": "/home/vodka/.nvm/versions/node/v24.15.0/bin/npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${input:github-token}"
      }
    }
  }
}
